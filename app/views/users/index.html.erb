<section class="users-section">
  <ul class="users-list">
    <% @users.each do |user| %>
      <li>
        Name: <%= user.name %>
        <span class="profile-link">
          <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
        </span>
        <span>
          <%= link_to 'Add friend', user_path(user,user_id: current_user.id, friend_id: user.id), method: :get, class: 'frnd-btn' %>
        </span>
      </li>
    <% end %>
  </ul>
</section>
