<section class="users-section">
  <ul class="users-list">
    <% @users.each do |user| %>
      <li>
        Name: <%= user.name %>
        <span class="profile-link">
          <%= link_to 'See Profile',  user_path(user), class: 'profile-link' %>
        </span>
        <span>
          <%= link_to 'Add friend', new_friendship_path(user_id: current_user.id, friend_id: user.id), method: :get, class: 'frnd-btn' %>
        </span>
      </li>
    <% end %>
  </ul>
</section>
